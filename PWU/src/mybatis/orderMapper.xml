<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="order">
	<insert id="insert" parameterType="vo.OrderVo">
		insert into order
		values(#{num},#{id},#{name},#{email},#{address},#{zipcode},#{reg_date},#{p_name},#{p_price},#{status})
	</insert>
	
<!-- 	<insert id="insert" parameterType="vo.OrderVo"> -->
<!-- 		insert into notice(num, subject, p_name, p_price, email, address, content) -->
<!-- 		values(notice_num.nextval,#{subject},#{p_name},#{p_price},#{email},#{address},#{content}) -->
<!-- 	</insert> -->
	
	<select id="selectList" resultType="vo.OrderVo" >
		SELECT * FROM order
	</select>
	

</mapper>